:root {
  --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  --secondary-gradient: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
  --dark-gradient: linear-gradient(135deg, #2c3e50 0%, #3498db 100%);
}

/* Global Reset */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background-color: #f8f9fa;
  color: #212529;
}

.container {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px;
  position: fixed;
  width: 100%;
  top: 0;
  background: var(--primary-gradient);
  z-index: 999;
}

#logo {
  height: 60px;
  width: 100px;
  background-image: url(https://png.pngtree.com/png-clipart/20230321/original/pngtree-food-business-logo-png-image_8998844.png);
  background-size: cover;
  background-position: center;
  border-radius: 10px;
}

#logo:hover {
  box-shadow: 0 10px 10px rgba(0, 0, 0, 0.2);
  transition: all 0.3s ease;
}

#sidelogo {
  display: flex;
  gap: 30px;
}

.heading {
  color: white;
  text-decoration: none;
  font-weight: bold;
  transition: all 0.3s ease;
}

.heading:hover {
  transform: scale(1.1);
  box-shadow: 0 10px 10px rgba(0, 0, 0, 0.15);
}

#body {
  margin-top: 80px;
  height: auto;
  padding: 50px;
  background-image: url(https://img.freepik.com/premium-photo/red-background-with-variety-food-including-burgers-fries-ketchup_916191-843.jpg);
  background-size: cover;
  background-repeat: no-repeat;
  background-position: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20px;
  border-radius: 10px;
}

.loki1, .loki2 {
  color: white;
  text-align: center;
}

.loki1 {
  font-size: 3rem;
}

.loki2 {
  font-size: 1.5rem;
}

.loki3 {
  background-color: #63ac63;
  color: white;
  font-weight: bold;
  border: none;
  border-radius: 25px;
  padding: 10px 20px;
  transition: all 0.3s ease;
}

.loki3:hover {
  background-color: #4b0532;
  transform: scale(1.1);
  box-shadow: 0 10px 10px rgba(0, 0, 0, 0.15);
}

#box {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 30px;
  padding: 50px;
  margin: auto;
  max-width: 1200px;
}

.sidebox {
  background-color: white;
  border-radius: 10px;
  overflow: hidden;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.sidebox:hover {
  transform: scale(1.05);
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
}

.img-box, .img-box2, .img-box3, .img-box4, .img-box5, .img-box6, .img-box7, .img-box8 {
  height: 200px;
  background-position: center;
  background-size: cover;
  border-radius: 10px;
}

/* Add remaining style blocks for service, gift, discount, footer, etc., with similar improvements */

/* Responsive Design */
@media (max-width: 768px) {
  .container {
    flex-direction: column;
    text-align: center;
  }
  #sidelogo {
    flex-direction: column;
    gap: 10px;
  }
  #box {
    padding: 20px;
    gap: 20px;
  }
}
